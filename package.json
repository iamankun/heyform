{
  "name": "heyform",
  "homepage": "https://heyform-webapp.vercel.app",
  "author": "An Kun.",
  "license": "AGPL-3.0",
  "private": true,
  "packageManager": "pnpm@9.12.1",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "scripts": {
    "dev": "pnpm --filter ./packages/webapp dev",
    "build": "pnpm --filter ./packages/webapp build",
    "preview": "pnpm --filter ./packages/webapp preview",
    "clean": "pnpm -r clean && rm -rf node_modules",
    "fix-json": "node scripts/fix-json.js",
    "validate": "node scripts/validate-json.js"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  },
  "workspaces": [
    "packages/*"
  ]
}